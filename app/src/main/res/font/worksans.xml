<?xml version="1.0" encoding="utf-8"?>
<font-family xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Thin (100) -->
    <font
        android:font="@font/worksans_thin"
        android:fontStyle="normal"
        android:fontWeight="100" />
    <font
        android:font="@font/worksans_thinitalic"
        android:fontStyle="italic"
        android:fontWeight="100" />

    <!-- ExtraLight (200) -->
    <font
        android:font="@font/worksans_extralight"
        android:fontStyle="normal"
        android:fontWeight="200" />
    <font
        android:font="@font/worksans_extralightitalic"
        android:fontStyle="italic"
        android:fontWeight="200" />

    <!-- Light (300) -->
    <font
        android:font="@font/worksans_light"
        android:fontStyle="normal"
        android:fontWeight="300" />
    <font
        android:font="@font/worksans_lightitalic"
        android:fontStyle="italic"
        android:fontWeight="300" />

    <!-- Regular (400) -->
    <font
        android:font="@font/worksans_regular"
        android:fontStyle="normal"
        android:fontWeight="400" />
    <font
        android:font="@font/worksans_italic"
        android:fontStyle="italic"
        android:fontWeight="400" />

    <!-- Medium (500) -->
    <font
        android:font="@font/worksans_medium"
        android:fontStyle="normal"
        android:fontWeight="500" />
    <font
        android:font="@font/worksans_mediumitalic"
        android:fontStyle="italic"
        android:fontWeight="500" />

    <!-- SemiBold (600) -->
    <font
        android:font="@font/worksans_semibold"
        android:fontStyle="normal"
        android:fontWeight="600" />
    <font
        android:font="@font/worksans_semibolditalic"
        android:fontStyle="italic"
        android:fontWeight="600" />

    <!-- Bold (700) -->
    <font
        android:font="@font/worksans_bold"
        android:fontStyle="normal"
        android:fontWeight="700" />
    <font
        android:font="@font/worksans_bolditalic"
        android:fontStyle="italic"
        android:fontWeight="700" />

    <!-- ExtraBold (800) -->
    <font
        android:font="@font/worksans_extrabold"
        android:fontStyle="normal"
        android:fontWeight="800" />
    <font
        android:font="@font/worksans_extrabolditalic"
        android:fontStyle="italic"
        android:fontWeight="800" />

    <!-- Black (900) -->
    <font
        android:font="@font/worksans_black"
        android:fontStyle="normal"
        android:fontWeight="900" />
    <font
        android:font="@font/worksans_blackitalic"
        android:fontStyle="italic"
        android:fontWeight="900" />
</font-family>